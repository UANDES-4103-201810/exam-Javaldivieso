<%= form_with(model: delivery_info, local: true) do |form| %>
  <% if delivery_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(delivery_info.errors.count, "error") %> prohibited this delivery_info from being saved:</h2>

      <ul>
      <% delivery_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :user_id, value: current_user.id %>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :delivery_info_address %>
  </div>

  <div class="field">
    <%= form.label :phone %>
    <%= form.number_field :phone, id: :delivery_info_phone %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
