<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :delivery_info_id %>
    <%= form.number_field :delivery_info_id, id: :order_delivery_info_id %>
  </div>

  <div class="field">
    <%= form.label :methodpay %>
    <%= form.text_field :methodpay, id: :order_methodpay %>
  </div>

  <div class="field">
    <%= form.label :pizzaorder_id %>
    <%= form.number_field :pizzaorder_id, id: :order_pizzaorder_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
